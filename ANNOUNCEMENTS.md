# dotsys 공지 운영 (`announcements.json`)

> 이 저장소의 `announcements.json` 한 파일이 앱 내 공지의 정본이다 (ADR-102).
> 앱(Windows·Android)이 부팅 시 + 6시간마다 이 파일을 읽어 배너로 보여준다.
> **빌드·배포·서버 무관** — 이 파일을 웹 UI 에서 편집(Commit)하면 끝.
> 상세 절차·문제 해결 = 비공개 문서 `docs/RECOVERY_RUNBOOK.md` **R10**.

## 게시 템플릿

`items` 배열 **맨 앞**에 아래 블록을 붙여넣고 값을 수정한다:

```json
{
  "id": "2026-08-10-keygen-rekey",
  "title": "알파 라이선스 키 재발급 안내",
  "content_text": "라이선스 시스템 개편으로 새 키를 이메일로 보내드렸습니다. 설정 > 라이선스에서 교체해 주세요.",
  "url": "https://dotsys.app/faq",
  "date_published": "2026-08-10T09:00:00+09:00",
  "_dotsys": {
    "level": "info",
    "expiresAt": "2026-10-01T00:00:00+09:00"
  }
}
```

## 값 규약 (오타 한 글자 = 아무에게도 안 뜬다)

| 필드 | 규칙 |
|---|---|
| `id` | `YYYY-MM-DD-slug`. **재사용 절대 금지** — 사용자 기기의 "읽음" 열쇠라서, 재사용하면 이미 닫았던 사용자에게 영영 안 뜬다. 내용 수정 재공지 = **새 id** |
| `title` | 80자 이내 (초과분 절단) |
| `content_text` | 500자 이내 (초과분 절단). **HTML 금지** — `content_html` 은 앱이 읽지 않는다 |
| `url` | http/https 만. 없으면 필드 생략 |
| `level` | `info`(다음 부팅에 표시) / `critical`(즉시 표시 + 경고톤). **이 두 단어만** — 다른 단어는 info 로 조용히 강등된다 |
| `platforms` | `["windows"]` / `["android"]` 소문자. **전 플랫폼이면 필드를 아예 빼라** — 오타 표면을 없애는 게 제일 안전하다 |
| `minAppVersion` / `maxAppVersion` | **반드시 `x.y.z` 세 자리** (`"0.5"` 는 비교 불가 → 전원 비노출). 확실치 않으면 빼라 |
| `startsAt` / `expiresAt` | ISO 8601 + 타임존(`+09:00`). `expiresAt` 지나면 자동 비표시 |
| 항목 수 | 최대 20건 — 오래된 것은 지워라 |

## 게시 직후 검증 (생략 금지)

1. 내 앱에서 **설정 > 공지 > "지금 확인"** (CDN 캐시로 ~5분 지연 가능).
2. 목록에 새 공지가 보이는가. `critical` 이면 배너도 즉시.
3. **"N건은 이 기기 대상이 아니에요"** 가 늘었다면 타겟팅 오타다 (`platforms` 철자·버전 형식).

## 회수 ("잘못 올렸다")

**⚠️ 파일 삭제 금지** — 앱은 404 에서 마지막 정상 피드를 계속 쓴다(fail-closed).
파일을 지우면 회수가 **영원히 전파되지 않는다.** 올바른 회수 2경로:

1. `items` 에서 해당 블록만 제거 후 Commit (빈 피드도 `"items": []` 로 유효 JSON 유지).
   실수 복구는 GitHub **Revert** 버튼.
2. 또는 해당 항목 `expiresAt` 을 과거 시각으로 변경 후 Commit.

전파: 종료 클라 = 다음 부팅 즉시 / 실행 중 = 최대 약 6.1시간 / 오프라인 = 다음 온라인까지.
급한 정정은 **새 id 로 정정 공지**를 추가 게시 + 이메일 병행.

## 절대 규칙

- JSON 파손 = 앱이 **피드 전체 무시**(마지막 정상 캐시 유지). Commit 전에 JSON 유효성 확인.
- 이 파일은 **공개**다 — 내부 정보·개인정보 금지.
